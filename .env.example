# PostgreSQL Database Configuration
POSTGRES_PASSWORD=changeme

# Optional: Override database connection (defaults use docker-compose values)
# DATABASE_URL=postgresql://tabletennis:changeme@postgres:5432/tabletennis
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=tabletennis
# DB_USER=tabletennis
# DB_PASSWORD=changeme

# Azure Blob Storage Backup Configuration (OPTIONAL)
# Backups are optional - PostgreSQL data persists in Docker volumes
# Only enable if you want off-site backups for disaster recovery
# ENABLE_BACKUPS=true
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string
# AZURE_STORAGE_CONTAINER_NAME=table-tennis-backups
# CRON_SCHEDULE=0 2 * * *
# BACKUP_ON_STARTUP=true
